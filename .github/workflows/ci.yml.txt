name: CI Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore Project/CardioTrack/CardioTrack/CardioTrack.sln

      - name: Build
        run: dotnet build Project/CardioTrack/CardioTrack/CardioTrack.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test Project/CardioTrack/CardioTrack/CardioTrack.sln --no-build --configuration Release --verbosity normal